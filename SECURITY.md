# 安全策略

## 报告漏洞

如果你发现本项目存在安全漏洞，**请不要开公开 Issue**，而是私下上报：

- 优先：使用 GitHub 的 [Security Advisories](../../security/advisories/new) 私密上报。
- 或：开一个标注 `security` 的私密 Issue（如仓库支持），或直接联系仓库维护者。

上报时请尽量包含：问题描述、复现步骤、影响范围、建议的修复方式。我们会在收到后尽快确认并跟进。

## 响应时间

- 首次确认：收到报告后 3 个工作日内。
- 修复与披露：根据严重程度评估，修复后在 advisory 中说明并致谢。

## 已知安全注意事项

请使用者在部署时自行关注以下点：

- **`config.yaml` 含真实 API token**（tushare / DeepSeek / Bocha / MX / Bark 等）。该文件已被 `.gitignore` 忽略，切勿提交。若曾误提交，须立即在各平台轮换相应密钥，并从 git 历史中清除。
- **持仓推送接口（`push`）无应用层鉴权**，明文 JSON。如需访问控制，请在消费方网关层做（见 `docs/integration/positions-push-api.md`）。
- **CORS 默认 `allow_origins=["*"]`**，生产部署须收紧到实际域名。
- 本项目为单用户个人工具，**未做多方鉴权 / 越权防护**，不要在多用户 / 公网暴露环境下直接运行 backend 而不加前置网关。

## 支持的版本

本项目为个人项目，安全修复仅针对 `main` 分支最新版本。
