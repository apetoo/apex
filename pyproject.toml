[project]
name = "apex"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "pytest>=8.0.0",
    "sse-starlette>=2.0",
    "uvicorn>=0.27",
]
