openai>=1.0.0
tushare>=1.4.25
akshare>=1.0.0
vectorbt>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
click>=8.0.0
pyyaml>=6.0
langgraph>=1.0,<2
langsmith>=0.4,<1

# ── 后端 API ──────────────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0

# ── Agent Skill / MCP ─────────────────────────────────────────────────────
# apex MCP server（让 Codex/Claude/OpenClaw 远程指挥 apex）。跑法见 README「七、MCP 接入」。
fastmcp>=3.0.0
requests>=2.31.0

# ── 缠论技术分析（/chan 页）────────────────────────────────────────────────
# pin 死版本：结构识别结果随版本漂移，golden 测试以 0.10.12 为准（2026-08-07 day-0 验证通过）
czsc==0.10.12

# ── 测试 ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
scrapy>=2.13,<2.14
